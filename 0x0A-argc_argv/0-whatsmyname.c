#include <stdio.h>

/**
 * main - print the name
 * @argv: arrays of argument
 * @argc: nb of argument
 * Return: Always 0 (success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
