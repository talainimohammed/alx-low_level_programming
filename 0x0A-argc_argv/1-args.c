#include <stdio.h>

/**
 * main - print the name
 * @argc: number arguments
 * @argv: array Arguments
 * Return: Always 0 (success)
 */
int main(int argc, char *argv[])
{
	printf("%i\n", argc - 1);

	return (0);
}
